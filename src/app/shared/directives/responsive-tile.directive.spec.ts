import { ResponsiveTileDirective } from './responsive-tile.directive';

describe('ResponsiveTileDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveTileDirective();
    expect(directive).toBeTruthy();
  });
});
